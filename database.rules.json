{
  "rules": {
    ".read": false,
    ".write": false,

    "boards": {
      "$board": {
        ".read": true,
        ".write": true,

        "$key": {
          ".validate": "newData.isString() && newData.val().length < 5500000"
        }
      }
    }
  }
}
